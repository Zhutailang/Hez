# Linux cloud: LiveKit with host networking (best for WebRTC UDP)
services:
  livekit:
    image: livekit/livekit-server:latest
    network_mode: host
    command: --config /etc/livekit.yaml
    volumes:
      - ./livekit.cloud.yaml:/etc/livekit.yaml:ro
    restart: unless-stopped
